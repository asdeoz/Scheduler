@if(Request.IsAuthenticated) {
    <text>¡Hi <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Log Off", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Log In", "LogOn", "Account") ] | [ @Html.ActionLink("Register", "Register", "Account") ]
}
